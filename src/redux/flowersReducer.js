
import allFlowers from './allFlowers';

const inicialState=[...allFlowers];

// eslint-disable-next-line import/no-anonymous-default-export
export default (state = inicialState, action) => {
    return state;
}
